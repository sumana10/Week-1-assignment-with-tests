/*
  Implement a class `DrawingBoard` with the following methods:
    - draw(shape): adds a shape to the drawing board
    - erase(index): erases the shape at the specified index
    - updateShape(index, updatedShape): updates the shape at the given index
    - getAllShapes: returns an array of all shapes on the drawing board
    - getShape(index): returns the shape at the specified index
    - clearAllShapes: deletes all shapes from the drawing board

  Test your implementation by running:
  - `npm run test-drawing-board`
*/
class DrawingBoard {
    // Your code here
}

module.exports = DrawingBoard;
